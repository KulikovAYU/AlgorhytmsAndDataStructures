language: cpp

before_script:
  choco upgrade cmake 3.20.0

compiler:
    - clang
    - gcc
    
addons:
   apt:
    packages:
      - cmake-data
      - cmake
      
script: 
- cmake .
- cmake --build . -- -j2
